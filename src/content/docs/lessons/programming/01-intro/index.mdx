---
title: Intro to Programming
description: What the programming subteam does and how these lessons work.
---
HI THIS IS AADEN TESTING THE CMS !



Welcome to the programming subteam! We write the code that drives the robot:
reading sensors, controlling motors, and automating everything the drivers
shouldn't have to think about.

## What you'll learn

* Java fundamentals for robot code
* WPILib, the FRC robotics library
* Version control with Git
* Debugging on real hardware

## Your first robot code

FRC robots run Java using WPILib. Here's the smallest useful example — a
command that spins a motor while a button is held:

```java
public class SpinIntake extends Command {
	private final IntakeSubsystem intake;

	public SpinIntake(IntakeSubsystem intake) {
		this.intake = intake;
		addRequirements(intake);
	}

	@Override
	public void execute() {
		intake.setSpeed(0.8);
	}

	@Override
	public void end(boolean interrupted) {
		intake.stop();
	}
}
```

Code blocks like the one above get syntax highlighting automatically — just
pick the language when you insert one.

## Adding images

Drop an image into the editor and it shows up right in the lesson. Use
screenshots liberally: a picture of the driver station beats three
paragraphs describing it.

> **Tip:** This lesson is the formatting template for the whole site. When
> writing your own lesson, copy the structure here: short intro, "what you'll
> learn," then the content in small sections.
